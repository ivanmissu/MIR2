# Delphi → Java translation map

| Delphi source | Java target | Status |
|---|---|---|
| `Common/Grobal2.pas:TDefaultMessage` | `protocol/DefaultMessage` | implemented + unit tested |
| `Common/Grobal2.pas:CM_/SM_ constants` | `protocol/ProtocolConstants` | mechanically extracted; field specifications still incomplete |
| `Common/EDcode.pas:Encode6BitBuf` | `protocol/SixBitCodec` | OLD mode implemented + unit tested; Delphi golden still required |
| `Common/EDcode.pas:EncodeMessage/DecodeMessage` | `protocol/MessageCodec` | implemented + unit tested |
| Delphi `AnsiString` / GBK fixed fields | `protocol/ByteStrings` | implemented + unit tested |
| `Common/DES.pas:EncryStr/DecryStr` | `protocol/DesCodec` | implemented with DES/ECB/NoPadding + zero padding; Delphi ciphertext golden still required |
| `LoginGate` / `SelGate` / `RunGate` socket boundary | `gate` | virtual-thread PoC and legacy framing implemented; limits, Netty and true-client validation remain |
| `M2Server/UsrEngn.pas:TUserEngine.Run` | `world/WorldEngine` | single-owner fixed tick and FIFO command queue implemented |
| `M2Server/Envir.pas:TMapHeader/TMapUnitInfo` | `world/Mir2MapLoader` | packed 52-byte header and 12-byte column-major cells implemented + unit tested |
| `M2Server/Envir.pas:CanWalk/MoveToMovingObject` | `world/GameMap` | terrain flags and moving-object occupancy implemented + unit tested |
| `M2Server/ObjBase.pas:WalkTo/RunTo/ClientChangeDir` | `world/WorldEngine` | eight-way walk/run/turn validation implemented; timing/golden comparison remains |
| `M2Server/ObjBase.pas:SearchViewRange/SendRefMsg` | `world/WorldEngine` | 12-cell square interest events (appear/move/turn/disappear) implemented |
| `RunGate` first RunLogin packet + `CM_TURN/WALK/RUN` | `gate` → `world` adapter | implemented + integration tested; mir2-front login/selection/entry smoke passed |
| `M2Server/ObjBase.pas:AttackDir` / `CheckActionInterval` | `world/WorldEngine.attack`, `AttackKind` | single-cell melee, shared CM_HIT interval, damage roll implemented |
| `M2Server/ObjBase.pas:RM_STRUCK/RM_DEATH/RM_HEALTHSPELLCHANGED/RM_WINEXP` | `gate/GameProtocolAdapter` | SM_STRUCK/SM_DEATH/SM_HEALTHSPELLCHANGED/SM_WINEXP mapped with TMessageBodyWL/TCharDesc |
| `M2Server/ObjMon*.pas` melee monster tick | `world/MonsterTemplate`, `WorldEngine.updateMonsters` | target acquisition, chase, attack intervals, corpse timeout implemented |
| `M2Server/ObjBase.pas:DropItemDown` / `ClientPickUpItem` | `world/ItemDrop`, `GroundItem`, `WorldEngine.pickUp` | drop table, SM_ITEMSHOW/SM_ITEMHIDE, CM_PICKUP implemented |
| `Envir/MonItems/*.txt` → `LocalDB.pas:LoadMonitems` / `UsrEngn.pas:1536 MonGetRandomItems` | `world/MonsterDropTable` (+ `resources/db/MonItems/`, GEEM2 baseline), `MonsterTemplate.goldDrops`, `WorldEngine.scatterGold` | probability/count/looks resolved from the real 1.76 tables; ordinary rows become `ItemDrop`; `金币` rows stay as `GoldDrop`, roll `Count div 2 + Random(Count)`, split through `DropGoldDown`-style ground piles (shape 112..116, 2000 cap/merge) and pickup calls wallet `IncGold` + `SM_GOLDCHANGED`; Delphi `FileExists` semantics: monsters without a table drop nothing |
| `M2Server/LocalDB.pas:LoadMonsterDB` → `UsrEngn.pas:2581` row mapping | `world/MonsterDb` (+ `resources/db/MonsterDb.tsv`, 378 rows), `MonsterTemplate.fromDb` | HP=MaxHP, AC=`MakeLong(wAC,wAC)` flat, DC=`MakeLong(DC,DCMAX)`, Level/Exp, walk/attack intervals from WALK_SPD/ATTACK_SPD with the 200ms floor; `MonsterWeapon=LoByte(MP)`; sight/view-range and behaviour family stay code-side `TODO(verify)` (no DB columns). The wired-in templates (10+木桩) are authoritative; the remaining rows import without behaviour and stand by for the 57-monster red line |
| `M2Server/ObjBase.pas:GetFeature` / `MakeHumanFeature` | `character/Character.feature`, GAME session | gender/hair/dress/weapon appearance persisted and packed into SM_LOGON Feature |
| `DBServer` ability/bag character record | `world/PlayerStateStore`, `persistence/SqliteStore` | Ability and ordered 46-slot backpack transactionally saved/restored; W02 SQLite migration tested |
| `M2Server/ItmUnit.pas:TItem` / `UsrEngn.pas:StdItemList` | `world/StdItem`, `world/ItemDatabase`, `world/StdItemsDb` + `resources/db/StdItemsDb.tsv` (684-row GEEM2 baseline), `persistence/SqliteStore.itemDatabase` | full authoritative catalog imported (684 unique, upstream duplicate names first-wins like Delphi); SQLite `std_items` seeds at boot and resolves instances by name like `CopyToUserItemFromName`; W04 placeholders and TODO(verify) values retired (`鹿肉` never existed in the real DB — drops now name `肉`); W19 split the packed `Reserved` byte back out from `NeedIdentify`, migrates W18 caches in place, and preserves 祈祷/赤血 flags on the `TClientItem` wire |
| `M2Share.pas:GetItemNumber` / `UsrEngn.pas:CopyToUserItemFromName` | `world/WorldEngine.allocateMakeIndex`, `world/BackpackItem.of` | per-instance MakeIndex seeded from the persisted high-water mark; instances created at full durability (Dura=DuraMax=template word) |
| `Common/Grobal2.pas:TStdItem` / `TClientItem` | `gate/ClientItemCodec` | 66-byte packed TStdItem + aligned MakeIndex → 76-byte little-endian TClientItem, 6-bit encoded; byte-offset unit tests; Delphi-captured golden still required |
| `M2Server/ObjBase.pas:ClientQueryBagItems` / `SendAddItem` | `gate/GameProtocolAdapter` | `CM_QUERYBAGITEMS → SM_BAGITEMS` (series=count, '/'-terminated entries, silent on empty bag) and `SM_ADDITEM` full TClientItem body implemented |
| `DBServer` inventory record with per-item MakeIndex/Dura | `persistence/SqliteStore` (character_inventory + std_items join) | make_index/dura/dura_max persisted per slot; W03 rows upgraded in place and renumbered by the engine on restore |
| `M2Server/Envir.pas:LoadMapData` door scan (`btDoorIndex`) | `world/Mir2MapLoader`, `world/DoorInfo` | `$80` anchors, index `&$7F >0`, TDoorStatus sharing inside ±10 same-index implemented + unit tested |
| `Common/Grobal2.pas:TDoorInfo/TDoorStatus`, `Envir.pas:GetDoor/ArroundDoorOpened` | `world/GameMap` door index & `aroundDoorOpened` | exact-anchor `doorAt` and the ±1 closed-door gate suppression implemented + unit tested |
| `M2Server/UsrEngn.pas:OpenDoor/ProcessMapDoor`, `ObjBase.pas:ClientOpenDoor` | `world/WorldEngine.openDoor` / `closeDoorsPeriodically`; `gate/GameProtocolAdapter` | `CM_OPENDOOR` silent open, `SM_OPENDOOR_OK`±12 broadcast, 500ms sweep + 5s auto-close `SM_CLOSEDOOR`; castle divergence (`bo01`, castle doors) noted |
| `M2Server/LocalDB.pas:QMapInfo` (MapInfo.txt) | `world/MapInfoLoader` | `loadmapinfo` includes (resolved from `MapInfo/` subdir), `;` comments, `[id|alias desc idx]` definitions, route lines with Delphi `GetValidStr3` delimiter chains and `Str_ToInt(x,0)` coercion; flags consumed but deferred |
| `M2Server/Envir.pas:AddMapRoute`, `ObjBase.pas:Walk/EnterAnotherMap` | `world/TeleportRoute`, `WorldEngine.addRoute` / gate crossing in `movePlayer` | routes registered only with both maps loaded; walk/run landing fires `SM_CLEAROBJECTS + SM_CHANGEMAP (+MAPDESCRIPTION)`; unwalkable destination rolls the whole move back; GateObj-on-closed-door suppression preserved |
| `M2Server/UsrEngn.pas:GetGameTime/ProcessHuman` (day/night cycle) | `world/WorldEngine.gameTimeFromHour/calculateDayBright/dayBright`, `WorldEvent.DayChanging`, `gate/GameProtocolAdapter` | 24-hour day/night cycle (day 1, twilight 2, dawn 0, night 3); DARK/DAYLIGHT flag overrides; SM_DAYCHANGING broadcast to online players |
| `M2Server/LocalDB.pas:QMapInfo` MapFlags (SAFE, DARK, DAY, NORECONNECT, MUSIC, EXPRATE, NOCHAT, QUIZ, FIGHT) | `world/MapFlags`, `world/MapInfoLoader` | complete map flag parsing from MapInfo.txt definitions, redirect on spawn for NORECONNECT, restriction enforcement (NOCHAT, QUIZ shout suppression) |
| `M2Server/ObjBase.pas:ClientSay/SendHearMsg/SendWhisper/SendShout` | `world/WorldEngine.say`, `WorldEvent.ChatHeard/Whisper/Shout/SystemMessage`, `gate/GameProtocolAdapter` | 12-cell local chat (`SM_HEAR`), `/target msg` whisper (`SM_WHISPER`), `!msg` map-wide shout (`SM_HEAR` with `(!)` prefix), system commands (`/who`, `@在线`), NOCHAT/QUIZ checks |
| Web frontend day/night & chat | `mir2-front` (`shared`, `bridge`, `web`) | day/night torch spotlight canvas lighting & twilight filter, character speech bubbles, chat input bar (CM_SAY), door interaction button, multi-client chat sync |
| `Common/Grobal2.pas:29-55` `U_*` constants, `THumanUseItems` (`array[0..12]`) | `world/EquipmentSlot`, `world/Equipment` | all 13 slots modelled; belt/boots/gem accept nothing because their `CheckUserItems` cases are commented out in the shipped 1.50 source |
| `M2Share.pas:3513 CheckUserItems` | `world/EquipmentSlot.accepts` | StdMode→slot matrix reproduced verbatim, including the three disabled slots |
| `M2Server/ObjBase.pas:22970 CheckTakeOnItems` | `world/EquipRequirement` + `WorldEngine.isLockedInPlace` | gender lock (StdMode 10/11), hand vs wear weight budget (`GetUserItemWeitht` excludes the target slot *and* both hand slots), `Need` 0/1/2/3/10/11/12/13; worn-item removal locks from `Reserved & 2` (default `m_boUserUnLockDurg=False`) and `Reserved & 4`; rebirth (4/40/41), castle requirements and `InDisableTakeOffList` refused/deferred as `TODO(verify)` |
| `M2Server/ItmUnit.pas:556 ApplyItemParameters` + `ObjBase.pas:2818 RecalcAbilitys` | `world/EquipmentBonus`, `WorldEngine.recalculateAbilities` | ItemType-driven stat mapping, accessory StdMode table, DC/MC/SC accumulated outside the case; Weight/WearWeight/HandWeight buckets; worn items with `Dura<=0` contribute nothing. Suite/special-ring flags (Shape/AniCount 111-217) deliberately not modelled |
| `M2Server/ObjBase.pas:17072 ClientTakeOnItems` | `world/WorldEngine.equip`, `gate/GameProtocolAdapter` | MakeIndex+`CompareText` bag lookup, slot/requirement checks, swap-back of the displaced item, `RecalcAbilitys`, `SM_TAKEON_OK(GetFeatureToLong, GetFeatureEx)` / `SM_TAKEON_FAIL(n18)` |
| `M2Server/ObjBase.pas:17221 ClientTakeOffItems` | `world/WorldEngine.unequip`, `gate/GameProtocolAdapter` | n10 codes -1/-2/-3/-4 preserved; **quirk reproduced**: the success path leaves `n10 = 0` and the exit test is `if n10 <= 0`, so a `SM_TAKEOFF_FAIL(recog=0)` always trails a successful `SM_TAKEOFF_OK` |
| `M2Server/ObjBase.pas:17300 ClientUseItems` + `23324 EatItems` | `world/WorldEngine.useItem` | StdMode 0-3 consumables with `IncHealthSpell` clamping and `Flag.boNODRUG`; StdMode 4 (books) and 31 (unpack) refused until the skill/container slices land |
| `M2Server/ObjBase.pas:16213 ClientDropItem` | `world/WorldEngine.dropItem` | safe-zone and `Flag.boNOTHROWITEM` gates, name split at the first space (`GetValidStr3`), `SM_DROPITEM_SUCCESS/FAIL` with recog=MakeIndex and the name as body |
| `M2Server/ObjBase.pas:16898 SendUseitems` | `gate/ClientItemCodec.encodeWornSet` | `IntToStr(slot) + '/' + EncodeBuffer(TClientItem) + '/'` per occupied slot; silent when nothing is worn |
| `Common/Grobal2.pas:734 TAbility` (50 bytes), `RM_ABILITY` | `gate/AbilityCodec` | packed little-endian layout with `MakeLong(min,max)` stat ranges; `MaxExp` now carries `GetLevelExp(Level)` (unsigned DWord, the 4e9 plateau wraps a signed int); weight words still zero (reported via SM_WEIGHTCHANGED); initial `SM_ABILITY` is now emitted immediately after map bootstrap so a fresh client has authoritative HP/MP/level/gold |
| `M2Server/ObjBase.pas:21521 WeightChanged` / `RM_WEIGHTCHANGED` | `WorldEvent.WeightChanged` → `SM_WEIGHTCHANGED` | recog=Weight, param=WearWeight, tag=HandWeight |
| `M2Server/ObjBase.pas:19992 GetFeature` (dress/weapon halves) | `WorldEngine.Player.feature()` | worn Shape overrides the dress/weapon bytes so take-on/off visibly changes the avatar; empty slots keep the character record's creation appearance |
| `DBServer` worn-item record | `persistence/SqliteStore` (`character_equipment`) | one row per occupied slot; pre-W12 databases gain the table on open; `itemMakeIndexHighWater` spans bag **and** equipment so a relog cannot re-issue a live MakeIndex |
| `M2Server/LocalDB.pas:777/793/798` `NODRUG` / `NOTHROWITEM` / `NODROPITEM` map flags | `world/MapFlags`, `world/MapInfoLoader` | parsed and enforced by the use/drop/death paths. `NOTHROWITEM` blocks a live player throwing items away; `NODROPITEM` (added in W20) is the separate flag `ScatterBagItems` reads — they are distinct in Delphi and were previously conflated |
| `M2Server/M2Share.pas:2216 g_dwOldNeedExps` + `ObjBase.pas:19184 GetLevelExp` | `world/LevelExperience` | levels 1..500 verbatim, 51+ share the 4,000,000,000 plateau (kept as `long` because it overflows a signed int); `> MAXLEVEL` clamps to the last entry, level 0 normalises to the level-1 requirement |
| `M2Server/ObjBase.pas:1869 RecalcLevelAbilitys` | `world/LevelAbilities` | warrior/wizard/taoist HP, MP, DC, AC and the three weight ceilings. **Quirks reproduced**: the warrior floors its DC minimum at 1 (casters at 0) and is the only job with natural AC (`MakeLong(0, L div 7)`). Delphi `Round` is banker's rounding, so the Java side uses `Math.rint`, not `Math.round`. `g_Config.nLevelValueOf*` divisors use their shipped defaults (6/2.5, 15/1.8, 4/4.5) |
| `M2Server/ObjBase.pas:1843 GetExp` + `1943 HasLevelUp` | `Ability.consumeLevelExperience`, `WorldEngine.awardExperience`/`applyLevelUp` | one threshold deducted per level-up, `MAXUPLEVEL = 65535` caps the level but keeps consuming experience, `MaxExp := GetLevelExp(Level)`, `RecalcLevelAbilitys` + `RecalcAbilitys`, then `IncHealthSpell(2000, 2000)` tops both pools up |
| `M2Server/ObjBase.pas:5584 RM_LEVELUP` | `WorldEvent.LevelUp` → `SM_LEVELUP` | recog=Exp, param=Level, no body; the full `SM_ABILITY` block follows, as in the Delphi branch |
| `M2Server/UsrEngn.pas:503` character-creation ability block | `Ability.defaultPlayer` | Level 1, HP/MP 15, DC 1-2, MaxExp 100. Delphi never runs `RecalcLevelAbilitys` at creation, so the warrior's DC visibly *narrows* from 1-2 to 1-1 at level 2 — reproduced, not smoothed |
| `M2Server/ObjBase.pas:20765 TBaseObject.Die` (player branch) | `WorldEngine.handleDeath` | death tick stamped before the scatter and the `RM_DEATH` broadcast |
| `M2Server/ObjBase.pas:26648 TPlayObject.ScatterBagItems` | `WorldEngine.scatterBagItems` | `Random(g_Config.nDieScatterBagRate{3}) = 0` per bag entry, `DropWide = 2`, dropped set reported via `RM_SENDDELITEMLIST`. W20 completed the two missing gates: `m_boAngryRing or m_boNoDropItem` (see `DropProtection`) exits before any roll, `g_Config.boDieRedScatterBagAll` + `PKLevel >= 2` drops the **whole** bag instead of a third, and the map gate is now the real `Flag.boNODROPITEM` rather than `NOTHROWITEM` |
| `M2Server/ObjBase.pas:15487 TPlayObject.DropUseItems` | `WorldEngine.dropUseItems` | two passes over the thirteen worn slots. **Pass 1** — `StdItem.Reserved and 8`: the item is destroyed outright, never dropped, and reported with an **empty name** (`DelList.AddObject('', MakeIndex)`), which is why `ItemRemoval` exists as a type separate from `BackpackItem`. **Pass 2** — `Random(nRate) = 0` with `nDieDropUseItemRate` 30, or `nDieRedDropUseItemRate` 15 once `PKLevel > 2`; a winning slot calls `DropItemDown(..., 2, True)` and is only cleared when `Reserved and 10 = 0` — **quirk kept verbatim: a `Reserved and 2` item lands on the floor *and* stays worn**. Gates: `m_boAngryRing or m_boNoDropUseItem` exits first; `Die` (ObjBase.pas:21002) then requires `boKillByMonstDropUseItem`(True)/`boKillByHumanDropUseItem`(False) to cover the killer, with an unattributed kill always scattering. **No map gate**: `DropUseItems` never reads `boNODROPITEM`, and `Die`'s own `(not m_boNoItem) or (not Flag.boNODROPITEM)` is an OR that a plain NODROPITEM map cannot satisfy. `InDisableTakeOffList` stays deferred (a server-config text list, not a catalogue column); `AddBodyLuck(-500)` and `MakeWeaponUnlock` need the luck / weapon-lock models |
| `M2Server/ObjBase.pas:2818 RecalcAbilitys` 护身/不掉物品 flags (2973, 3010-3012, 3158-3160, 3274) | `world/DropProtection` | `m_boAngryRing` (blocks both penalties), `m_boNoDropItem` (bag only), `m_boNoDropUseItem` (gear only). Slot asymmetry preserved: weapon/right-hand/dress read the code off `AniCount`, every other slot off `Shape`; codes 170/171/172, plus the classic 117 form for 护身 which only exists on the hand/dress branch and the two ring slots. Zero-durability items grant nothing |
| `M2Server/ObjBase.pas:2234 PKLevel` / `19197 GetNamecolor` / `19013 GetCharColor` | `world/PkLevel` | `PKLevel = m_nPkPoint div 100`; colour 255 白 → `btPKLevel1NameColor` $FB at level 1 → `btPKLevel2NameColor` $F9 at level 2+. **Order quirk**: `m_boPKFlag`'s $2F tint overrides the level colour, but only while `PKLevel < 2`, so a red name stays red through a duel. Guild/castle/FIGHT3 colour branches are deferred with those systems |
| `M2Server/ObjBase.pas:2362 IncPkPoint` / `18893 DecPKPoint` / `4042 TBaseObject.Run` decay | `WorldEngine.addPkPoint`, `WorldEngine.decayPkPoints` | a murder adds `nKillHumanAddPKPoint` 100 (one whole level); `dwDecPkPointTime` 2 minutes bleeds `nDecPkPointCount` 1 back off, clamped at zero. **Broadcast quirk**: `DecPKPoint` repaints only when the derived level moved *and* the **old** level was 1 or 2 — a 深红 character falling 3 → 2 stays silently red. The GM `@IncPkPoint` path (`CmdIncPkPoint`, 13211) always repaints |
| `M2Server/ObjBase.pas:21220 SetPKFlag` / `18868 CheckPKStatus` | `WorldEngine.setPkFlag` (called from `applyDamage`) | trading blows puts the **attacker** into the $2F colour for `dwPKFlagTime` 60 seconds, refreshed rather than stacked. The whole block is guarded by the victim's `not m_boPKFlag`, and skipped when either side is already red or the fight is inside a FIGHT zone. Transient: never persisted, always clear at login |
| `M2Server/ObjBase.pas:20938-20953 Die` (murder branch) | `WorldEngine.applyMurderPenalty` | with the shipped defaults (`boKillHumanWinLevel/Exp` False, no guild war, no castle): the killer gains 100 PK points and both sides get the GBK lines `g_sYouMurderedMsg`「你犯了谋杀罪...」/`g_sYouKilledByMsg`「你被%s杀害了...」. Skipped in a FIGHT zone or when the victim is already red; a flagged victim makes it `IsGoodKilling`, which only sends「[--你受到正当规则保护--]」and costs nothing |
| `M2Server/ObjBase.pas:2263 RefNameColor` → `RM_CHANGENAMECOLOR`(8090) | `WorldEvent.NameColorChanged` → `SM_CHANGENAMECOLOR`(656) | recog = the object whose name changed, param = the colour byte from `GetCharColor`; broadcast to every observer and to the player itself |
| `HumData.nPKPOINT` (ObjBase.pas:24904) | `PlayerState.pkPoint`, `persistence/SqliteStore` (`character_state.pk_point`, pre-W20 databases default 0) | the murder counter travels with the character record and is migrated in place; `m_boPKFlag` deliberately does not persist |
| `M2Server/ObjBase.pas:22857 SendDelItemList` / `RM_SENDDELITEMLIST` | `WorldEvent.ItemsRemoved(List<ItemRemoval>)` → `SM_DELITEMS` | series=count, body=`<name>/<MakeIndex>/` repeated. W20 widened the payload from `BackpackItem` to `world/ItemRemoval`, a `(name, makeIndex)` pair that **allows an empty name** — `DropUseItems` really does put `/MakeIndex/` on the wire for `Reserved and 8` destructions, and the client matches on MakeIndex alone |
| `M2Server/ObjBase.pas:21199 ReAlive` + `13998 CmdReAlive` | `WorldEngine.revive` → `SM_ALIVE` | stands up on the same cell with HP refilled; recog=object, param/tag=cell, series=direction, body=8-byte `TCharDesc` |
| `M2Server/ObjBase.pas:20510 MakeGhost` (trigger at 3769) | `WorldEngine.makeGhostsOfExpiredCorpses` | `dwMakeGhostTime` = 3 minutes, after which the corpse leaves the map |
| `M2Server/UsrEngn.pas:600` load-time revive | `WorldEngine.enter` | a character stored at `HP <= 0` re-enters at 14 HP; the Delphi home-map relocation needs the multi-map spawn table and is not modelled |
| `M2Server/ObjBase.pas:3718 TBaseObject.Run` (regen branch) | `WorldEngine.regenerateHealthAndSpell` | counters advance by `elapsed div 20`; `nHealthFillTime` 300 (6s) restores `MaxHP div 75 + 1`, `nSpellFillTime` 800 (16s) restores `MaxMP div 18 + 1`; the dead branch skips regeneration entirely |
| `M2Server/ObjBase.pas:10848 CmdChangeLevel` (GM `@Level`) | `WorldEngine.setLevel` | sets the level outright and runs `HasLevelUp(1)`; capped at `MAXUPLEVEL` |
| `M2Server/ObjBase.pas:2250 CalcGetExp` (high-level kill decay) | not modelled | `TODO(verify)`: needs per-monster levels, which `Monster.DB` has not supplied yet |
| `M2Server/ObjBase.pas:AttackDir` weapon wear / `StruckDamage` armour wear (W13 catch-up row) | `WorldEngine.damageEquipment`, `wearArmorOnStruck` | hit costs the weapon `Random(5)+2`, being struck costs the dress `Random(10)+5` plus every occupied slot a 1-in-8 chance of the same; zero durability destroys the item and clears its slot (`SM_DURACHANGE`, `SM_DELITEMS`); `RM_DURACHANGE` carries recog=Dura, param=slot, tag=LoWord(DuraMax), series=HiWord(DuraMax) |
| `M2Server/ObjBase.pas:3750 TBaseObject.Run` HP=0 revival branch | `WorldEngine.tryRevivalRing` | gates in Delphi order: last hitter `m_boUnRevival`, `m_boRevival`, `GetTickCount - m_dwRevivalTick > dwRevivalTime`(60s, strictly greater). `m_dwRevivalTick` starts at 0, so the first 60s of world uptime are inside the cooldown — same property as Delphi relative to OS uptime. On success: `ItemDamageRevivalRing`, `HP := MaxHP`, `SM_HEALTHSPELLCHANGED`, green hint `g_sRevivalRecoverMsg` 「复活戒指生效，体力恢复.」 |
| `M2Server/ObjBase.pas:3625 ItemDamageRevivalRing` | `WorldEngine.consumeRevivalRings` | every worn item whose Shape is revival-capable — or, for the two hand slots only, whose AniCount is — pays a flat 1000 durability; **no `break`, two rings both pay for one revival**; zero-durability items are destroyed (slot cleared, `SM_DELITEMS` from the pre-clear slot, `RecalcAbilitys`) rather than returned to the bag; the `RM_DURACHANGE` follow-up fires only when `Round(old/1000) <> Round(new/1000)` — banker's rounding, so 2500→1500 sends nothing |
| `M2Server/ObjBase.pas:2866/2968/3218-3228/3271 RecalcAbilitys` `m_boRevival` / `m_boUnRevival` flags | `WorldEngine.equipmentGrantsRevival` / `preventsRevival` | weapon/right-hand/dress slots grant `m_boRevival` through AniCount 114/160/161/162, every other slot through Shape 114/160/161/162; items at zero durability grant nothing. **Asymmetry preserved**: a dress with AniCount 114 sets the flag but is never consumed by `ItemDamageRevivalRing`. Shape 144 (accessory branch only) sets `m_boUnRevival` on the wearer and suppresses the *victim's* ring revival — PvP only, monsters wear nothing |
| `M2Server/ObjNpc.pas:1516 TMerchant.UserSelect` (label dispatch) | `WorldEngine.selectMerchantLabel` → `WorldEvent.MerchantRepairDialog` → `SM_SENDUSERREPAIR` | `m_sScriptLable` state machine without the script engine: labels must start with `@`, `@repair`/`@s_repair` (CompareText, case-insensitive) answer `SM_SENDUSERREPAIR(recog=echoed merchant id)` so the client opens its repair dialog; other labels are stored silently. a **W21 minimal NPC slice** now places visible stand-in NPCs (`WorldEngine.spawnNpc`, feature `MakeMonsterFeature(RC_NPC=50, 0, wAppr)` → client `TNpcActor`), which occupy their cell, appear through the ordinary SM_TURN sight flow and answer `CM_QUERYUSERNAME`; `FindMerchant`/Market_Def dialogues and trading remain deferred (red line on Market_Def) |
| `M2Server/ObjNpc.pas:2385 ClientQueryRepairCost` | `WorldEngine.queryRepairCost` → `SM_SENDREPAIRCOST` | quote = `Round(price div 3 / DuraMax × (DuraMax - Dura))` (integer division before the real math, banker's rounding), ×3 for a special repair; no price / full durability answers -1 (client prints ???); a MakeIndex+name that matches nothing in the bag stays silent, exactly like the Delphi `Exit` |
| `M2Server/ObjNpc.pas:2423 ClientRepairItem` | `WorldEngine.repairItem` → `SM_USERREPAIRITEM_OK/FAIL` | normal repair: `Dec(DuraMax, (DuraMax - Dura) div 30)` then `Dura := DuraMax`; special repair keeps DuraMax. **Quirk reproduced**: the charge triples `nPrice` *before* the `div 3` (cancelling out → un-truncated base) while the quote triples the truncated base afterwards, so the two disagree by 1-2 gold whenever `price mod 3 ≠ 0`. StdMode 43 refused; `DecGold` failure leaves the item untouched; the client refreshes gold/Dura/DuraMax from `SM_USERREPAIRITEM_OK(recog=gold, param=Dura, tag=DuraMax)`. `GotoLable` script follow-up and castle tax are deferred |
| `TPlayObject.m_nGold` + `UsrEngn.pas:2310` login restore + `UserLogon` `nTestGold` floor (ObjBase.pas:16360) | `PlayerState.gold`, `persistence/SqliteStore` (`character_state.gold`, pre-W15 databases default 0), `ServerConfig.testGold` (`MIR2_TEST_GOLD`) | wallet persisted with the character record, capped at `nHumanMaxGold` 10,000,000; the test-server floor tops the wallet up at login (default 0 = no-op) and — deviating from silent Delphi — announces it with `SM_GOLDCHANGED` after `MapEntered` so test consoles see it immediately |
| `M2Server/ObjBase.pas:5685 RM_ABILITY` header (`m_nGold`, `MakeWord(btJob, 99)`) | `WorldEvent.AbilityChanged(gold, job)` → `gate/GameProtocolAdapter.sendAbility` | the client refreshes its gold display from every `SM_ABILITY` (ClMain.pas:4198), so Recog carries the wallet and Param the packed job; both were previously hardcoded to zero, which zeroed the client-side gold on each ability refresh |
| `M2Server/ObjBase.pas:1648 GoldChanged` / `RM_GOLDCHANGED` | `WorldEvent.GoldChanged` → `SM_GOLDCHANGED` | recog = the new wallet total; game-gold words stay zero (not modelled) |
| Magic system, NPC script engine, guild/castle colour branches | next/future slices | not started; W12 closed equip/use/drop, W13 durability, W14 level/death/revival, W15 repair + revival rings + gold, W18 imported the authoritative Monster.DB/StdItems.DB/MonItems data, W19 restored `TStdItem.Reserved`/take-off lock semantics, and **W20 closed `DropUseItems` + the PK-point/name-colour model + `boDieRedScatterBagAll`**; Market_Def script engine itself remains red-lined until real-client 对拍 |
| Client-side stress: no Delphi equivalent (new Java-side tooling) | `loadtest/` module: `LoadtestMain` (CLI), `BotWireClient` (real `#…!` frames + prefix rotation + 12-byte little-endian header + 6-bit body, GBK), `Mir2Bot` (login→create→enter→walk/hit/pickup→periodic relogin state machine), `BotSwarm` (ramped start, live monitor line, verdict), `BotMetrics`/`BotReport` (markdown+csv), `BotSwarmEmbeddedTest` (CI) | the swarm speaks the same wire protocol as `mir2.exe`; it replaces a room full of human testers, not the golden-vector work |
| Traffic capture & golden replay: no Delphi equivalent (new Java-side tooling, P0 三件套之一/二) | `wiretool/` module: `WireToolMain` (record/replay/inspect CLI), `RecorderProxy` + `FrameSplitter` (transparent byte relay with `#…!`/noise classification into `.mrec` v1), `RecordingCodec` (truncation-tolerant append format), `Replayer` (paced session replay), `ReplayDiff` (identical/content/missing/extra/skipped), `ReplayReport` (Chinese markdown+csv), `FrameDescriber` (ProtocolConstants reflection, RunLogin with masked cert) | skeleton delivered with loopback/bot-driven evidence; awaiting Windows+mir2.exe Delphi-side captures to become byte-level goldens |
| Shadow comparison harness: no Delphi equivalent (the last unstarted P2 row, G3 hard requirement) | `shadowdiff/` module: `ShadowDiffMain` (embedded/remote CLI), `Op` (one-line script dialect: turn/walk/run/hit/heavyhit/bighit/pickup/bag/say/drop/eat/takeon/takeoff/sleep/**tick**/relog), `ShadowWireClient` (the same `#…!` + RunLogin + `+GOOD/+FAIL` dialect as the bot swarm), `ShadowSession` (three-gate login, per-op quiet-window drain, wire-derived StateSnapshot: cell/dir/HP/MP/level/gold/bag/worn with MakeIndex normalised out), `ShadowDiff` (STATE > ACKS > MESSAGES severities, message multiset order-normalised), `ShadowReport` (Chinese markdown+csv, exit code is the verdict) | skeleton delivered and self-checked: embedded dual-Java comparison PASS (29-step smoke incl. relog persistence round trip), divergence detection proven by a deliberately offset spawn; remote mode is ready to point at a Delphi LoginGate the moment the Windows environment exists |

| `GetTickCount` — read from every cadence in the server (`ObjBase.pas` 310 sites, `UsrEngn.pas` 87, `ObjMon.pas` 79, `Envir.pas` 5), notably `TMonster.Run` walk interval (ObjMon.pas:437 `Integer(GetTickCount - m_dwWalkTick) > m_nWalkSpeed`), attack interval (ObjMon.pas:392 `> m_nNextHitTime`), think/search stamps (ObjMon.pas:254/258), `RegenMonsters` (`UsrEngn.pas:19 dwRegenMonstersTick`), and the decay/regen stamps `TBaseObject.Initialize` writes (ObjBase.pas:20095) | `world/WorldClock` (`SYSTEM` / `VIRTUAL` / `MANUAL`) injected into `WorldEngine` behind the existing `LongSupplier clock`; `ServerConfig.MIR2_WORLD_CLOCK`; `WorldEngine.advanceTicks(int)`; `runTickBody()` split out of `tickOnce()` | `SYSTEM` is production and is bit-for-bit the previous behaviour (`System.currentTimeMillis()`). `VIRTUAL`/`MANUAL` derive time as `epoch + ticks × tickMillis` so every interval check becomes a pure function of the tick counter — the precondition for comparing *moving* monsters across two processes, which `WorldRandom` alone cannot give (it pins *which* roll comes Nth, not *how many* rolls happen). Epoch defaults to 1,000,000 rather than 0 because `TBaseObject.Initialize` stamps its cooldown windows with the current tick and then tests `GetTickCount - stamp > interval`; a zero epoch would place the world's first seconds inside every cooldown at once. **Deliberate deviation, accepted:** Delphi reads `GetTickCount` live *inside* a tick, so a slow tick lets a monster consume the "owed" time in one pass; virtual mode quantises to the tick grid and never accumulates owed time. Unobservable at the shipped 50 ms tick (every engine interval is ≥200 ms = ≥4 ticks) but real under load — hence production stays on `SYSTEM`. `MANUAL` additionally makes `tickOnce()` skip `runTickBody()` (commands still drain) so the periodic half cannot race an inbound command into a different observation bucket |
| `@tick [N]` — **no Delphi counterpart**, harness-only GM command | `WorldEngine.pumpTicks` via `processSay`; `Op.Kind.TICK` / `tick <N>` in the shadowdiff script dialect | advances a `MANUAL` world by N ticks (default 1, capped at `MAX_PUMPED_TICKS`=100,000 ≈ 83 min of virtual time at 50 ms, because each pumped tick is a full tick body on the world thread) and replies `SM_SYSMESSAGE` `"@tick N -> T ticks, now=M"`. Refused outright on `SYSTEM`/`VIRTUAL` worlds, i.e. inert on every production server. The reply carries the new world time so a divergence in how many ticks actually ran surfaces as a state difference instead of drifting silently |

## G0 evidence status

- [x] 12-byte packing and legacy codec skeleton
- [x] 20 local deterministic round-trip vectors
- [x] Linux container/JDK build baseline
- [x] World tick, map collision, lifecycle and movement visibility unit tests
- [ ] Delphi-captured byte-for-byte vectors
- [ ] Unmodified client login and RunLogin
- [ ] Unmodified client movement broadcast
- [x] One monster, kill, drop and pickup in deterministic unit/integration tests
- [x] Local relog persistence of HP/MP/level/experience/bag across SQLite and World restart
- [x] Full 76-byte TClientItem encoding plus `CM_QUERYBAGITEMS → SM_BAGITEMS` in deterministic tests (byte-offset layout derived from the Grobal2.pas field list)
- [ ] Real-client TClientItem/SM_BAGITEMS relog validation
- [ ] 50-bot one-hour stability run — harness delivered and scaled evidence in: 50×5 min PASS (0 errors) in both embedded and remote-fatjar modes (reports in `g0-evidence/`) plus a 50×2 min gate per PR in CI; the full 1 h run reproduces via `--duration 1h` and its report will be appended
- [ ] Delphi-vs-Java shadow comparison — the `shadowdiff` harness is delivered and self-checked (embedded dual-Java 29-step smoke PASS, divergence detection proven, CI gate per PR); the actual Delphi-side run needs the Windows + mir2.exe environment and then reduces to `--left-host <delphi> --right-host <java>`
- [x] Deterministic comparison of *moving* monsters (W23) — `--ai` boots both sides on a `MANUAL` world clock and paces them with `tick` ops: 18-step PASS with an auditable observation trace (monsters chase, strike for 8 HP, the player strikes back, HP regenerates, the census survives a relog), negative control `--right-seed 99999` FAILs at `hit 6` (dmg 2 vs 1). Boundary scenario (AI decision landing exactly on an op boundary): wall clock 5/5 FAIL with drifting difference counts, tick-paced equivalent 5/5 PASS. Cross-implementation use still blocked: Delphi has no `@tick` equivalent
